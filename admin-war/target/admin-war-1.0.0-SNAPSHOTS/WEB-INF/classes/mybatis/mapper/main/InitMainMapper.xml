<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.mzhong.janytask.admin.center.mapper.main.InitMainMapper">

    <select id="isInited" resultType="boolean">
        select count(*) from sqlite_master where type = 'table' and name = 'ja_user'
    </select>

    <update id="initTables">
        create table ja_user(
          username varchar(128) primary key not null,
          password char(32) not null,
          name varchar(128),
          email varchar(128)
        )
    </update>
</mapper>